run this as a bookmarklet or as a local webpage to poll ebay to see if it has an item.
If the item is present, turn the screen green and make a noise.


## compiling a new version of the javascript from the coffeescript (correct as of 2012-07-28):
  1. Install Coffeescript
  2. install jslint
  3. run `coffee -o build/ -cwl src/` from the project directory
	-l  use the linter
	Use `coffee -j build/ebaypoller.js -cwl src/`   to compile into one .js

